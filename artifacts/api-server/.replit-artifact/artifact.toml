kind = "api"
previewPath = "/api"
title = "API Server (Flask)"
version = "1.0.0"
id = "3B4_FFSkEVBkAeYMFRJ2e"

[[services]]
localPort = 8080
name = "API Server"
paths = ["/api"]

[services.development]
run = "python app.py"

[services.production]

[services.production.build]
args = []

[services.production.run]
args = ["gunicorn", "--bind", "0.0.0.0:8080", "app:app"]

[services.production.run.env]
PORT = "8080"

[services.production.health.startup]
path = "/api/healthz"
