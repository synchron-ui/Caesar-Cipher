# Use this when deploying the frontend to a static host.
# If the backend is hosted separately, set the absolute URL below.
# If frontend is deployed with the backend on the same domain, leave it empty.
VITE_API_BASE_URL=https://your-backend-host.com
